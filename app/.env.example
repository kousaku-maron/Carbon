VITE_API_BASE_URL=http://localhost:8787
# Set to "true" to enable polling-based token exchange (for dev without deep links)
VITE_AUTH_EXCHANGE=true
# Set to "true" to enable cloud upload for dropped/pasted images
VITE_ENABLE_CLOUD_IMAGE_UPLOAD=false
